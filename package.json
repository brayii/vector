{
  "name": "vector-project-learning",
  "private": true,
  "type": "module",
  "scripts": {
    "learning": "node scripts/run-python.cjs .project-learning/framework/lifecycle.py",
    "test": "node scripts/run-python.cjs -m unittest discover -s .project-learning/tests -p test_*.py && node scripts/run-python.cjs -m unittest hangar.test_hangar"
  },
  "engines": { "node": ">=24" }
}
